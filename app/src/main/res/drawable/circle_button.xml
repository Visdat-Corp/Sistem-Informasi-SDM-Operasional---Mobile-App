<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Transparan di tengah -->
    <solid android:color="@android:color/transparent" />

    <!-- Garis pinggir -->
    <stroke
        android:width="3dp"
        android:color="#2196F3" /> <!-- Ganti warna sesuai kebutuhan -->

    <!-- Ukuran tombol -->
    <size
        android:width="60dp"
        android:height="60dp"/>
</shape>
